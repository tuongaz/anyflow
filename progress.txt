## Codebase Patterns
- Biome `organizeImports` sorts strictly alphabetically by source string. `@/...` imports come BEFORE third-party because `@` < `r`. `bun run format` does NOT auto-rewrite import order (it's a "suggested" Biome fix that requires `--unsafe`); manually reorder so `bun run lint` passes.
- `ColorTokenSchema` is the single source of truth for the palette enum (declared in `apps/studio/src/schema.ts`); the same string-literal union is mirrored in `apps/web/src/lib/api.ts` as `ColorToken`.
- Tests run under `bun test` and use the `bun:test` import (`describe/it/expect`). Example: `apps/web/src/lib/connector-to-edge.test.ts`.
- Web workspace TS config: `@/*` resolves to `apps/web/src/*`; uses `react-jsx`; `noEmit: true`; `tsc --noEmit` for typecheck.
- The `prd.json` and progress files are at the repo ROOT, not inside a `ralph/` subdirectory (the Makefile's `ralph-clean` target predates this — keep an eye on it).
- Demo file write pattern (apps/studio/src/api.ts): every mutation reads + parses + mutates the *raw parsed JSON* (not the Zod-typed object) so unknown forward-compat fields survive round-trips, then re-validates the WHOLE demo via `DemoSchema` before commit, then writes via `writeFileAtomic` inside `withDemoWriteLock(demoId, …)`. Output is `JSON.stringify(obj, null, 2) + '\n'` — 2-space indent + trailing newline. Errors use a discriminated `Outcome` union mapped to HTTP status in a final `switch`.
- Body-validation pattern: top-level mutation bodies are validated with a Zod schema using `.strict()` to reject typos in unknown keys; final shape is enforced via `DemoSchema.safeParse(rawAfterMutation)`. Nested fields can stay loose (`z.unknown()`) because the post-mutation DemoSchema check is the real gate.
- `crypto.randomUUID()` is available globally under Bun (no import needed). New node ids follow `node-${crypto.randomUUID()}`; new connector ids follow `conn-${crypto.randomUUID()}`.
- Connector PATCH gotcha: the connector schema is a `z.discriminatedUnion('kind', ...)`. By default, `z.object` strips unknown keys on parse, so a stale `eventName` (left over from a kind='event' → kind='default' change) would parse cleanly but linger on disk. PATCH /connectors/:connId clears `method/url/eventName/queueName` when `kind` changes — keep this invariant if you generalize the merge helper.
- Frontend hook tests run as pure-function tests under `bun:test` — there's no React testing library installed. When writing a stateful hook, extract the state-update logic into pure helpers (e.g. `applySetOverride`, `applyPruneAgainst`) and test those; the hook itself is a thin `useState`/`useCallback` shell. Pattern: `apps/web/src/hooks/use-pending-overrides.ts` exports both the hook and the underlying pure reducers.
- Optimistic-edit reconciliation: `usePendingOverrides<T>()` is the single way the canvas overlays in-flight edits on server data. `setOverride(id, partial)` BEFORE the API call; on success, the next `demo:reload` triggers `pruneAgainst(snapshot)` which drops fields that match the server (per-field, not per-entity); on failure, `dropOverride(id)` reverts. Use this pattern for every new UI mutation (US-022 onward) — don't add ad-hoc `useState` maps.
- Frontend type sync: `apps/web/src/lib/api.ts` mirrors the studio's `apps/studio/src/schema.ts` Demo types as plain TS interfaces (no zod on the frontend). When schema additions land server-side, mirror them in `api.ts`. Discriminated unions in `api.ts` use inline-typed branches so the variant-name TS types don't collide with the React component names of the same name (e.g. `PlayNode` is a component in `nodes/play-node.tsx` — don't also export a `PlayNode` type from api.ts; build the union from anonymous branches instead).
- Discriminated union spread gotcha: `mergeNodeOverride(node: DemoNode, override: Partial<DemoNode>)` in `apps/web/src/components/demo-canvas.tsx` returns a `DemoNode`, but TS can't preserve the variant through `{...node, ...override, data}` because `Partial<DemoNode>` flattens `data` to a union of partials. The override is keyed by the entity id so it's always the same variant — safe to cast at the boundary (`as DemoNode`). Future merges of partial-of-discriminated-union should follow the same trick.
- Studio schema is loaded once at process start. After modifying `apps/studio/src/schema.ts`, restart the studio (`bun run apps/studio/src/cli.ts stop` then `... start --daemon`) before browser-verifying. The demo files will round-trip through the new schema on the next `demo:reload`. Live-reload only covers the frontend bundle (Vite HMR); backend changes need a restart.
- NodeResizer wiring (US-023): each custom node component renders `<NodeResizer isVisible={selected && !!data.onResize} minWidth={80} minHeight={40} onResizeStart={…} onResizeEnd={…}/>` inside its outer card. `data.onResize` + `data.setResizing` are injected by `DemoCanvas` via the `data` field of the React Flow Node spec (same channel as `onPlay`). On resize-stop, the node fires `data.onResize(id, {width,height})` which lands in `DemoView.onNodeResize` → optimistic `setNodeOverride(id, { data: { width, height } })` → `updateNode(demoId, id, dims)` PATCH. Inner cards switch to `h-full w-full` while resizing OR when `data.width/data.height` are set; otherwise they keep natural sizing (`w-[260px]` for play/state, per-shape inline dims for shape). DemoCanvas blocks the `setRfNodes(sourceNodes)` echo while either `draggingRef` OR `resizingRef` is true (mirrors the position-drag pattern).
- Inspector selection state (US-024): `DemoView` owns BOTH `selectedId` (node) and `selectedConnectorId` (connector) as separate `useState`s, but they're mutually exclusive — `onSelectNode(id)` clears the connector and vice versa. `DemoCanvas` accepts both and wires React Flow's `onNodeClick` + `onEdgeClick`; `onPaneClick` clears both. Edges get `selected: true` when their id matches `selectedConnectorId` (assigned to the `DerivedEdge` after `connectorToEdge` returns). `DetailPanel` accepts both `node` and `connector` props; the Sheet is open when either is set, and switching between them keeps the Sheet mounted.
- Tabbed sidebar (US-024): `DetailPanel` wraps both node and connector content in `<Tabs value={tab}>` (Detail + Style). `tab` lives in a local `useState<TabKey>` and is reset to `'detail'` whenever the selected entity id changes (`useEffect` keyed on `selectionId = node?.id ?? connector?.id`). This means tab choice persists across SSE-driven re-renders that keep the same selection, but a brand-new selection always opens on Detail. Style tab contents are intentional placeholders for now — US-025 fills them in. New shadcn primitive at `apps/web/src/components/ui/tabs.tsx` (depends on `@radix-ui/react-tabs`, added via `bun add` in the `apps/web` workspace).
- Hooks-order trap when adding inspector state (US-025): `apps/web/src/pages/demo-view.tsx` has TWO early returns (`if (!summary)`, `if (loading && !detail)`) before the render block. ANY `useMemo` / `useState` / `useCallback` added later in the function MUST go ABOVE those early returns or React throws "Rendered fewer hooks than expected" on the first render where the guard fires. The pattern is: keep all hooks at the top of the component, then `if (!summary) return …`, then plain consts + JSX. When adding a derived `useMemo` (e.g. merging overrides into the selected entity), promote `const demo = detail?.demo` ABOVE the guards too so the memo deps don't reach into post-guard locals.
- Connector edge color flow (US-025): `colorTokenStyle(token, 'edge')` returns `{ stroke }` and is composed into the edge style alongside the dasharray in `apps/web/src/lib/connector-to-edge.ts`. When the connector has no explicit `color` token, the edge's stroke is left undefined so React Flow's default selection styling stays intact. When you ADD optimistic-style flows for new connector fields, add them to the `mergeConnectorOverride(c, override)` helper in `demo-canvas.tsx` AND to the `selectedConnector` `useMemo` in `demo-view.tsx` — both render paths need to see the in-flight edit (the canvas for the visual, the inspector for the active-swatch state).
- Inline editing surface (US-026): `apps/web/src/components/inline-edit.tsx` is the canonical text editor for any in-place edit on the canvas. Pattern: render a `<button>` for the visible text with `onDoubleClick` setting an `editing` state to a discriminator ('label' | 'description' | true), and conditionally swap to `<InlineEdit>` while editing. Add `nodrag nopan nowheel` to opt-out of React Flow's pointer/wheel capture; add `e.stopPropagation()` inside `onKeyDown` so Backspace/Delete/Escape don't bubble to the canvas keyboard listener. For required fields (e.g. PlayNode/StateNode label per the schema's `min(1)`), pass `required=true` so empty values revert with the shake animation instead of firing a 400-error PATCH.
- Custom React Flow edge with HTML label (US-026): connector edges use a custom `editableEdge` type (`apps/web/src/components/edges/editable-edge.tsx`) instead of React Flow's built-in `smoothstep`. The visual output is identical because the custom edge calls `getSmoothStepPath` for the path; the difference is the label is rendered through `EdgeLabelRenderer` (an HTML overlay portal) instead of an SVG `<text>`, so it can swap to an `<input>` on double-click. Position the overlay with `transform: translate(-50%, -50%) translate(${labelX}px, ${labelY}px)`. Register the type in `demo-canvas.tsx`'s module-scope `edgeTypes` constant (mirrors the `nodeTypes` pattern) — `<ReactFlow edgeTypes={edgeTypes}>` must be passed a stable reference or it triggers a console warning.
- Description = detail.summary (no new field): inline-editing a node's description writes `data.detail.summary`, NOT a sibling `data.description` field (per US-016 + US-026). The handler in `demo-view.tsx` must SPLICE the new summary into the existing `detail` object: `{ ...(node.data.detail ?? {}), summary }`. Sending `{ summary }` alone would lose `fields[]`/`dynamicSource`/`filePath`. The PATCH body schema's `detail: z.unknown()` accepts the whole object; the post-mutation `DemoSchema.safeParse` is the real gate.
- Invisible resize affordance (US-031): the canonical resize UI is `apps/web/src/components/nodes/resize-controls.tsx` — eight `<NodeResizeControl>` (4 lines + 4 corners) styled fully transparent with wider hit areas (8px lines, 12px corners). The cursor IS the affordance, wired by React Flow's built-in `.react-flow__resize-control.{position}` CSS. Each control has a `<span data-testid='resize-control' data-position='...'>` for tests. Use `<ResizeControls visible={selected && !!data.onResize && !isEditing} ...>` inside any custom node component instead of `<NodeResizer>`. To prevent Radix Sheet/Dialog from closing on resize-control pointerdown, pass `onInteractOutside={(e) => { if ((e.target).closest('.react-flow__resize-control')) e.preventDefault(); }}` to `SheetContent`.
- Pointer-event handlers + closures (US-028): handlers attached via React's `onPointerDown`/`onPointerMove`/`onPointerUp` close over the render's state at registration time. A single synchronous gesture (down→move→up in one task) doesn't get a re-render between events, so the LATER handlers still see STALE state from the closure. Mirror gesture state into refs (`drawShapeRef`/`drawStartRef`/`drawCurrentRef`) and read from refs in the handlers; keep React state for visual artifacts (the ghost preview). Also: `setPointerCapture` / `releasePointerCapture` THROW on synthetic (non-trusted) events used in browser tests — wrap both calls in `try/catch` so the failure doesn't abort the rest of the handler. Symptom of the missing try/catch: gesture appears to "stick" (draw mode doesn't exit, no commit fires) because the throw propagates up through React's `invokeGuardedCallback` and skips the commit logic.
- Radix Popover inside a non-modal Sheet (US-032): when a Popover's trigger lives inside `<SheetContent modal={false}>`, the popover's content renders into a portal *outside* the SheetContent. Clicking inside the popover registers as an "outside" interaction for the Sheet, which would close it. Guard `SheetContent.onInteractOutside` by exempting any target inside `[data-radix-popper-content-wrapper]` (the wrapper Radix puts around floating content): `if (target?.closest('[data-radix-popper-content-wrapper]')) e.preventDefault();`. Use the same hook to exempt other in-canvas gestures (e.g. `.react-flow__resize-control` from US-031). The Popover itself handles ESC and outside-click-on-popover-content to close — Radix's nested DismissableLayer logic does the right thing once you stop the Sheet from also reacting.

---

## 2026-05-07 - US-018
- Implemented the color palette token system end-to-end.
- Files added:
  - `apps/web/src/lib/color-tokens.ts` — single-source map `COLOR_TOKENS` keyed by every `ColorToken` enum value with `{ border, background, edge }` CSS strings, plus a `colorTokenStyle(token, kind)` helper with overloads:
    - `kind: 'node'` → `{ borderColor, backgroundColor }` (spread into a node container's `style`)
    - `kind: 'edge'` → `{ stroke }` (spread into a React Flow edge `style`)
    - `undefined` token falls back to `'default'` (which uses shadcn HSL CSS vars so it adapts to dark mode).
  - `apps/web/src/lib/color-tokens.test.ts` — covers: every enum value has a mapping, the helper returns non-empty strings for each token, default-token uses CSS vars, undefined falls back to default, and the helper output matches the `COLOR_TOKENS` map.
- Quality checks: `bun test` (105/0), `bun run typecheck` (clean), `bun run lint` (clean).
- **Learnings for future iterations:**
  - Biome's `organizeImports` is a "suggested" fix — `bun run format` doesn't apply it. Run `bun run lint` to surface it, then manually reorder. `@/`-prefixed imports come BEFORE third-party (alphabetical by literal source string).
  - Schema additions for US-016/US-017 already exported `ColorTokenSchema` from `apps/studio/src/schema.ts` AND mirrored a `ColorToken` string-union in `apps/web/src/lib/api.ts`. US-018 just bridged the two with a CSS-value mapping; no schema or API change needed.
  - The PRD's helper signature is ambiguous about return shape ("returns the right CSS values"). I used overloads returning a `CSSProperties`-shaped object so call sites can `style={{ ...colorTokenStyle(token, 'node') }}` directly. The test's "returns a non-empty string for each token" maps onto each property of that object being non-empty.
  - For the colored tokens I inlined Tailwind palette HSL triplets (border ≈ 400, background ≈ 50/100, edge ≈ 500). The `default` token uses `hsl(var(--border))` / `hsl(var(--card))` / `hsl(var(--muted-foreground))` so it inherits the active theme.
- Manual browser verification: not required by US-018 (no UI rendering yet — that lands in US-022/US-025).
---

## 2026-05-07 - US-019
- Implemented backend node CRUD endpoints in `apps/studio/src/api.ts`:
  - `PATCH /api/demos/:id/nodes/:nodeId` — partial update of position, label, detail, borderColor, backgroundColor, width, height, shape. Body validated by a new `NodePatchBodySchema` (`.strict()` so unknown keys are rejected). `position` lands on `node.position`; every other key lands inside `node.data` via the new `mergeNodeUpdates` helper.
  - `POST /api/demos/:id/nodes` — appends a complete new node payload. Auto-generates `id = 'node-' + crypto.randomUUID()` when absent. Returns `{ ok, id }`.
  - `DELETE /api/demos/:id/nodes/:nodeId` — removes the node and cascade-removes every connector with `source === nodeId || target === nodeId` in the SAME atomic write.
- All three endpoints share the same shape: 400 on body / on-disk JSON / schema failure, 404 on unknown demoId or nodeId, 500 on write failure. They all run inside `withDemoWriteLock` and write via `writeFileAtomic`. Crucially they re-validate the WHOLE mutated demo via `DemoSchema` before commit (existing `/position` endpoint stays as-is — pure-position mutations can't break invariants so the pre-mutation parse is enough there).
- Existing `PATCH /api/demos/:id/nodes/:nodeId/position` fast path preserved unchanged. Hono's radix tree picks `/:nodeId/position` over `/:nodeId` when both exist.
- Files changed:
  - `apps/studio/src/api.ts` — new endpoints, `NodePatchBodySchema`, `mergeNodeUpdates` helper, `ColorTokenSchema` import.
  - `apps/studio/src/api.test.ts` — three new `describe` blocks (15 new tests): PATCH partial update, position-only update, schema validation 400, body-strict 400, label-empty 400, 404s, file formatting preservation; POST creates with auto-id, with caller-id, schema-failure 400, 404; DELETE cascade (full removal), DELETE with non-cascading connectors preserved, 404s.
- Quality checks: `bun test` (121/0 — was 105 before, +16 incl. one shared describe restructure), `bun run typecheck` (clean), `bun run lint` (clean).
- **Learnings for future iterations:**
  - Mutate the *raw parsed JSON* (the `unknown` from `Bun.file().json()`), NOT the Zod-typed result. The schema strips/coerces — author-written fields outside the schema would be lost on round-trip. The existing `/position` handler already followed this pattern; new endpoints must too.
  - `DemoSchema.superRefine` enforces connector → node referential integrity. Cascade-deleting connectors when a node is removed isn't optional — without it the post-mutation `DemoSchema.safeParse` would 400 on every node delete that had any adjacent connectors.
  - Biome's formatter wraps long lambda bodies onto a single line when they fit. The DELETE handler had a 3-line `obj.connectors.filter((cn) => …)` that the formatter rewrote inline. `bun run format` fixes this; always run `bun run format` before `bun run lint`.
  - `crypto.randomUUID()` works at the global scope under Bun — no import needed.
  - For PATCH bodies, prefer `.strict()` Zod schemas over loose `z.record(z.unknown())` — strict catches client typos (e.g. `borderColour` instead of `borderColor`) at the body-validation layer rather than waiting for the post-mutation schema parse to throw a more confusing error.
- Manual browser verification: not required by US-019 (backend-only). UI consumers land in US-021 onward.
---

## 2026-05-07 - US-020
- Implemented backend connector CRUD endpoints in `apps/studio/src/api.ts`, mirroring the US-019 node CRUD pattern:
  - `PATCH /api/demos/:id/connectors/:connId` — partial update of `label`, `style`, `color`, `direction`, `kind`, `eventName`, `queueName`, `method`, `url`. Body validated by `ConnectorPatchBodySchema` (`.strict()`). When `kind` changes, the new `mergeConnectorUpdates` helper drops every kind-specific field (`method/url/eventName/queueName`) before applying the new ones, so the on-disk JSON doesn't carry phantom fields from the previous kind.
  - `POST /api/demos/:id/connectors` — creates a new connector. Auto-generates `id = 'conn-' + crypto.randomUUID()` when absent. Defaults `kind` to `'default'` (the no-semantics user-drawn variant) when absent. DemoSchema's `superRefine` enforces source/target referential integrity automatically.
  - `DELETE /api/demos/:id/connectors/:connId` — removes the connector. Unlike node DELETE, no cascade is needed (connectors don't own anything else).
- All three endpoints share the same Outcome-union → switch-on-kind pattern: 400 on body / on-disk JSON / schema failure, 404 on unknown demoId or connectorId, 500 on write failure. They run inside `withDemoWriteLock(id, …)`, write via `writeFileAtomic`, and re-validate the WHOLE demo through `DemoSchema` before commit.
- Files changed:
  - `apps/studio/src/api.ts` — three new endpoints, `ConnectorPatchBodySchema`, `ConnectorKindSchema`, `mergeConnectorUpdates` helper, `CONNECTOR_KIND_FIELDS` constant.
  - `apps/studio/src/api.test.ts` — three new `describe` blocks (14 new tests): PATCH visual-fields merge, kind-change clears stale fields, schema 400 (kind='event' without eventName), body-strict 400, 404s; POST default-kind auto-id, caller-id pass-through, schema 400 (unknown source), kind-discriminated 400, demo 404; DELETE removes targeted, demo 404, conn 404.
- Quality checks: `bun test` (135/0 — was 121 before, +14), `bun run typecheck` (clean), `bun run lint` (clean).
- **Learnings for future iterations:**
  - `z.discriminatedUnion` with `z.object` children silently STRIPS unknown keys on parse but does NOT remove them from the source object you mutated. If you change the discriminator (`kind`) without dropping the previous variant's fields, the post-parse object looks correct but the on-disk JSON keeps the stale fields. Always clear kind-specific fields explicitly when the discriminator changes.
  - For POST endpoints with auto-generated IDs, prefer a typed-prefix convention (`node-`, `conn-`) — it makes the PRD's "id auto-generated if absent" testable via a regex match (`expect(id).toMatch(/^conn-/)`) without hardcoding UUIDs in fixtures.
  - The post-mutation `DemoSchema.safeParse` is the real gate. The body-validation schema only catches typos and obvious type errors at the top level; per-kind invariants (e.g. `kind='event'` requires `eventName`) are caught by the discriminated union's per-branch parsing of the *whole* demo. This means the body schema can stay loose on per-kind fields without losing safety.
- Manual browser verification: not required by US-020 (backend-only). UI consumers (Style tab editor, connector creation via handle drag) land in US-024/US-025/US-029.
---

## 2026-05-07 - US-021
- Generalized the `positionOverrides` reconciliation pattern into a reusable hook + pure reducers, then refactored `demo-view.tsx` to consume it for both nodes and connectors. Position-drag UX is preserved byte-for-byte — drag-stop still fires `setOverride(nodeId, { position })` → PATCH → `pruneAgainst` on the next `demo:reload` echo or `dropOverride` on PATCH failure.
- Files added:
  - `apps/web/src/hooks/use-pending-overrides.ts` — `usePendingOverrides<T extends { id: string }>()` returning `{ overrides, setOverride, dropOverride, pruneAgainst, reset }`. State-update logic lives in three pure exported helpers (`applySetOverride`, `applyDropOverride`, `applyPruneAgainst`) so tests can run without a React test renderer (none is installed under `apps/web`). `pruneAgainst` is per-FIELD: it drops every override key whose value `deepEqual`s the server's value, then drops the entry if nothing remains. `setOverride` shallow-merges so multi-field optimistic edits accumulate.
  - `apps/web/src/hooks/use-pending-overrides.test.ts` — 15 tests covering: set + merge + last-write-wins + immutability, drop existing + drop missing (reference-stable), prune-on-match (single + multi-field + entity-missing-from-snapshot + multi-id), drop-on-error (revert).
- Files modified:
  - `apps/web/src/pages/demo-view.tsx` — replaced `positionOverrides` `useState` + `useEffect` reconciliation with two `usePendingOverrides<DemoNode>()` + `usePendingOverrides<Connector>()` hooks. New `data-testid='edit-error-banner'` (replaces `position-error-banner`). Position-drag callback is now `setNodeOverride(nodeId, { position }) → PATCH → dropNodeOverride on failure`.
  - `apps/web/src/components/demo-canvas.tsx` — `positionOverrides: Record<id, {x,y}>` prop replaced with `nodeOverrides: OverrideMap<DemoNode>`. New module-scoped `mergeNodeOverride(node, override)` helper does shallow merge with `data` shallow-merged one level deeper. The merge is read at render time inside the existing `sourceNodes` `useMemo`, so position-drag still flows through React Flow's internal state during the gesture.
- Quality checks: `bun test` (150/0 — was 135 before, +15 new tests in `use-pending-overrides.test.ts`), `bun run typecheck` (clean), `bun run lint` (clean).
- **Learnings for future iterations:**
  - `apps/web` has no React testing library. Test stateful hooks by extracting the state-update logic into pure helpers and testing those. The hook then becomes a trivial `useState`/`useCallback` shell that doesn't need a unit test of its own (the React types ensure `setState(reducer)` is called correctly).
  - Per-field pruning (vs per-entity) is the right generalization. If a user changes both `position` AND `label` and only the position lands first (e.g. the label PATCH is still in flight), per-entity pruning would wipe the label override on the position echo. Per-field keeps each override alive until its own server value matches.
  - `Object.entries(prev)` vs `Object.keys(prev)` + `prev[id]` matters under TS strict mode. The keyed lookup is typed `Partial<T> | undefined` because of `Record`'s index-signature semantics, which trips `noUncheckedIndexedAccess`. Use `entries` to get a non-optional `[id, partial]` tuple.
  - When generalizing an existing optimistic-state pattern, the test surface should mirror the pattern's full lifecycle: set → prune-on-match (success path), set → drop (failure path), set + set (multi-field), prune-against-missing (entity gone). Anything less and you'll regress an edge case the next time someone changes the merge logic.
  - The position-drag flow's "drag-while-moving lives in React Flow's internal state" trick (`draggingRef` + the `useEffect` guard) still works after the generalization because the override map is keyed by id and only flows through React Flow's `nodes` prop on render — not on every drag event.
- Manual browser verification: not required by US-021 (no new visual surface — the position-drag UX is unchanged and the new `edit-error-banner` won't be exercised until US-024/US-025/US-026/US-029 wire actual mutating UI). The data-testid rename (`position-error-banner` → `edit-error-banner`) is intentional per the PRD; CLAUDE.md still references the old name in the position-drag pattern paragraph but the new banner subsumes it.
---

## 2026-05-07 - US-022
- Implemented the decorative ShapeNode component (rectangle / ellipse / sticky) and registered it in `DemoCanvas`.
- Files added:
  - `apps/web/src/components/nodes/shape-node.tsx` — `ShapeNode` React component for `type: 'shapeNode'`. Three visual variants driven by `data.shape`:
    - rectangle: `rounded-lg border-2 bg-transparent` — themed border, transparent bg by default.
    - ellipse: `rounded-full border-2 bg-transparent` — themed border, transparent bg by default.
    - sticky: `rounded-md border shadow-md -rotate-1` — defaults to amber bg, slight rotation + shadow to read as a sticky note.
    - Honors `data.borderColor` + `data.backgroundColor` via `colorTokenStyle(token, 'node')`. Sticky's amber default is overridable: any explicit `backgroundColor` token wins.
    - Honors `data.width` / `data.height`; falls back to per-shape default sizes (`rectangle/ellipse: 200×120`, `sticky: 180×180`).
    - Renders a `<Handle>` on each side (top/right/bottom/left). Top + Left are `type='target'`; Right + Bottom are `type='source'`. Handle classes match the existing `play-node.tsx`/`state-node.tsx` styling.
    - `data-testid='shape-node'` + `data-shape` attribute for tests.
- Files modified:
  - `apps/web/src/lib/api.ts` — added `NodeVisual` (width/height/borderColor/backgroundColor), `ShapeKind`, `ShapeNodeData`. Rewrote `DemoNode` from a single interface into a discriminated union over `type` (`playNode | stateNode | shapeNode`) using anonymous branch types so the new union doesn't collide with the existing `PlayNode`/`StateNode` React component names.
  - `apps/web/src/components/demo-canvas.tsx` — registered `shapeNode: ShapeNode` in the module-scope `nodeTypes` object (still a stable reference). `mergeNodeOverride` now ends with `as DemoNode` because TS can't preserve the discriminator through the spread of `Partial<DemoNode>`.
  - `apps/web/src/components/detail-panel.tsx` — narrowed `node?.data.detail` access via `node.type !== 'shapeNode' ? node : null` so shape nodes (which have no `detail`/`fields`/`dynamicSource`) typecheck cleanly. Decorative nodes get a minimal panel surface; the proper Style tab lands in US-024/US-025.
- Quality checks: `bun test` (150/0, no new tests — visual-only story), `bun run typecheck` (clean), `bun run lint` (clean).
- Manual browser verification: added three temp shapeNodes (rectangle/ellipse/sticky) to `examples/todo-demo-target/.anydemo/demo.json`, restarted studio (schema cache), navigated to `/d/todo-demo`, verified all three render correctly with their expected sizes, colors, and side-handle dots; reverted demo.json after.
- **Learnings for future iterations:**
  - The web app's `DemoNode` was previously a single interface — adding `shapeNode` forced it into a discriminated union. That ripples to every consumer: `demo-canvas.tsx`'s `mergeNodeOverride` and `detail-panel.tsx`'s `node?.data.detail` both broke until narrowed/cast. Worth checking every `node.data.<x>` access when the union widens again.
  - `apps/web/src/lib/api.ts` mirrors the studio's `Demo` types as plain TS interfaces (no zod on the frontend). When `apps/studio/src/schema.ts` grows new fields, mirror them in `api.ts` in the same PR — the typecheck won't complain about missing fields (they'd just round-trip as unknown), but the UI can't read them.
  - Don't export per-variant types `PlayNode`/`StateNode`/`ShapeNode` from `api.ts` — they collide with the React component names from `apps/web/src/components/nodes/`. Build the discriminated union from anonymous branches: `(NodeBase & { type: 'foo'; data: FooData })`.
  - Studio schema is process-cached — after editing `apps/studio/src/schema.ts`, restart the studio before browser-verifying. The first browser hit on a demo with new node/connector kinds will otherwise fail with `Invalid discriminator value`.
  - The PRD's "ellipse" is `rounded-full` (Tailwind), which produces a capsule when width != height. A true ellipse requires SVG. The PRD prescribes `rounded-full`, so capsule is the intended visual.
  - `data.label` on `ShapeNodeData` is optional — the component renders nothing when absent, which is the desired "blank decorative shape" affordance.
---

## 2026-05-07 - US-023
- Wired React Flow's `NodeResizer` into all three custom node components (PlayNode, StateNode, ShapeNode) so any selected node can be resized from any of the four corner handles or four edge lines. Resize-while-dragging stays in React Flow's internal state via the `dimensions` NodeChange flow; we only PATCH on resize-stop, mirroring the existing position-drag pattern.
- Files modified:
  - `apps/web/src/lib/api.ts` — added `UpdateNodeBody` type + `updateNode(demoId, nodeId, patch)` helper that PATCHes the generic `/api/demos/:id/nodes/:nodeId` endpoint (existing `updateNodePosition` stays for the position-only fast path).
  - `apps/web/src/components/demo-canvas.tsx` — added `onNodeResize` prop. New `resizingRef` (alongside the existing `draggingRef`) blocks the `setRfNodes(sourceNodes)` echo while a resize is in flight, so an SSE `runs` tick during the gesture doesn't snap the node back. `onResize` + `setResizing` callbacks are injected into each node's `data` (same channel `onPlay` uses). When `merged.data.width/height` are present, the React Flow Node spec gets explicit `width`/`height` so the wrapper sizes the inner.
  - `apps/web/src/components/nodes/play-node.tsx`, `state-node.tsx`, `shape-node.tsx` — render `<NodeResizer isVisible={selected && !!data.onResize} minWidth={80} minHeight={40} onResizeStart={…} onResizeEnd={…}/>`. Outer card switches between natural sizing (`w-[260px]` for play/state, per-shape inline dims for shape) and `h-full w-full` (post-resize OR while a resize is live, tracked via local `useState` `isResizing`). Inner content uses `flex-1` so the description body fills the remaining vertical space when the node has been resized taller than its natural content.
  - `apps/web/src/pages/demo-view.tsx` — added `onNodeResize` callback wired into `DemoCanvas`. Sets a pending override `{ data: { width, height } }` BEFORE the PATCH (cast at the boundary to `Partial<DemoNode>` because TS can't see through the discriminated union). On PATCH failure, drops the override and surfaces the existing `edit-error-banner`.
- Quality checks: `bun test` (150/0 — no new tests; this story is visual-only per the PRD), `bun run typecheck` (clean), `bun run lint` (clean).
- Manual browser verification: studio was already running (dev mode, Vite HMR). Navigated to `/d/todo-demo`, clicked the `api-create-todo` PlayNode and confirmed 4 corner handles + 4 edge lines render (none rendered when unselected). Repeated for `todo-worker` StateNode — same behavior. Performed an actual drag on the bottom-right handle of `api-complete-todo` from (850, 258) to (985, 353) using Playwright's drag tool; node resized smoothly to 327×97, demo.json was rewritten with `data.width: 327, data.height: 97` (verified via `grep`), no SSE-driven snap-back. Reverted demo.json afterward.
- **Learnings for future iterations:**
  - `NodeResizer` from `@xyflow/react` works by dispatching `dimensions` NodeChanges — `applyNodeChanges` then sets `rfNode.width` / `rfNode.height` AND React Flow's wrapper element gets `style.width` / `style.height` from those. The inner node component MUST use `h-full w-full` (or `100%/100%` inline) for the visible card to follow the wrapper. If the inner has a fixed Tailwind width (`w-[260px]`), the wrapper grows but the inner stays small — a confusing visual. We solved it with conditional sizing: `sized = isResizing || data.width !== undefined || data.height !== undefined ? 'h-full w-full' : 'w-[260px]'`.
  - Local `useState` `isResizing` (in addition to the canvas-level `resizingRef`) is needed in each node so the inner switches to `h-full w-full` on the FIRST resize gesture (before any `data.width` lands). Without it, the first drag starts with an unsized inner and the gesture looks broken until resize-stop persists `data.width/height`.
  - When extending the position-drag-blocks-echo pattern (the `draggingRef` in DemoCanvas), use a parallel `resizingRef` AND let nodes toggle it via a `data.setResizing` callback. Avoid triggering React state changes for "is this node resizing?" at the canvas level — re-renders during a gesture would force React Flow to re-do its measurement and worsen the UX.
  - Biome's `organizeImports` puts `type NodeProps` before `NodeResizer` in `from '@xyflow/react'` because alphabetical sort is by the imported name (ignoring the `type` keyword). Run `bun run lint` to surface this; the auto-fix is "suggested" so manual reorder is required.
  - The `data` channel (the `Record<string, unknown>` slot of a React Flow Node) is the canonical place to inject per-node callbacks (`onPlay`, `onResize`, `setResizing`). Don't add a parallel context or prop drilling — the existing pattern (`onPlay` since US-007) already proves it works for SSE-tick stability.
  - `updateNode(demoId, nodeId, patch)` is the generic frontend PATCH helper for `/api/demos/:id/nodes/:nodeId`. Existing `updateNodePosition` stays as the position-only fast path (per the US-019 acceptance criteria). Future UI mutations (label, colors, detail) should funnel through `updateNode` rather than adding a new helper per field.
---

## 2026-05-07 - US-024
- Restructured `DetailPanel` into a tabbed inspector (Detail / Style) and added connector selection so the same Sheet now hosts either a node or a connector.
- Files added:
  - `apps/web/src/components/ui/tabs.tsx` — shadcn-style wrapper around `@radix-ui/react-tabs` (Root, List, Trigger, Content). Mirrors the tone of `dialog.tsx` (forwardRef + `cn(...)`).
- Files modified:
  - `apps/web/package.json` — added `@radix-ui/react-tabs` (v1.1.x, brought in via `bun add` from inside the workspace; `bun add --filter` from the repo root tries to resolve `@anydemo/web` against npm and 404s).
  - `apps/web/src/components/detail-panel.tsx` — new `connector: Connector | null` prop alongside `node`. Sheet is open when either is set. Both branches render a `<Tabs>` with two triggers (`detail-panel-tab-detail` + `detail-panel-tab-style`); active tab lives in a local `useState<TabKey>('detail')` and resets to `'detail'` whenever the selected entity id changes (`useEffect` keyed on `node?.id ?? connector?.id`). Connector branch shows a minimal `ConnectorSummary` (source/target/kind/label/style/color/direction + per-kind URL/event/queue) on Detail. Style tab content is a `StylePlaceholder` for both — US-025 will replace it with real swatch rows + delete button. All existing data-testids on the Detail tab kept verbatim (`detail-panel`, `detail-panel-title`, `detail-panel-dynamic`, `detail-panel-refresh`, `detail-panel-dynamic-skeleton`, `detail-panel-recent-events`, `detail-panel-recent-event`, `detail-panel-run`).
  - `apps/web/src/components/demo-canvas.tsx` — new `selectedConnectorId` + `onSelectConnector` props. `onEdgeClick` fires `onSelectConnector?.(edge.id)`. `onPaneClick` now clears BOTH node and connector selection. Edges get `selected: true` when their id matches `selectedConnectorId` (mutated on the `DerivedEdge` after `connectorToEdge` returns).
  - `apps/web/src/lib/connector-to-edge.ts` — added optional `selected?: boolean` to `DerivedEdge`. The flag is set in `DemoCanvas`, not at edge creation time, so the typed surface stays minimal.
  - `apps/web/src/pages/demo-view.tsx` — new `selectedConnectorId` state; new `onSelectNode` / `onSelectConnector` callbacks that clear the *other* selection (mutually exclusive). `selectedConnector` is derived from `demo?.connectors.find(c => c.id === selectedConnectorId)` and passed to `DetailPanel`. `onClose` clears both selection ids.
- Quality checks: `bun test` (150/0, no new tests — visual-only story per PRD), `bun run typecheck` (clean — required adding `selected?: boolean` to `DerivedEdge`), `bun run lint` (clean), `bun run format` (autoformatted two files).
- Manual browser verification: studio in dev mode at `localhost:4321`. Navigated to `/d/todo-demo`, clicked the `api-create-todo` PlayNode → Detail tab selected by default with full existing content; clicked Style tab → placeholder rendered. Clicked the `c-complete-worker` connector edge (mid-line at ~905,280) → panel switched to "publishes todo.completed · event", Detail tab reset (was Style); clicked Style → connector placeholder rendered. Clicked the `api-complete-todo` PlayNode → panel switched again, Detail tab reset (was Style on the connector). Confirmed: tabs persist within a selection but reset on new selection, both nodes AND connectors drive the same Sheet.
- **Learnings for future iterations:**
  - `bun add @radix-ui/react-tabs` from the **repo root with `--filter @anydemo/web`** fails because bun tries to resolve `@anydemo/web` against the npm registry first and 404s. Run `bun add` from inside the workspace directory (`cd apps/web && bun add @radix-ui/react-tabs`) — bun then writes both the workspace `package.json` AND the root `bun.lock` correctly.
  - React Flow's `Edge` type accepts a `selected: boolean` field that drives its built-in selection styling (default blue stroke). The repo's hand-rolled `DerivedEdge` type didn't include it; adding it as optional was the smallest safe change. Don't try to push selection state into `data` — React Flow specifically reads the top-level `selected` field.
  - For two related selections that share a Sheet host (node + connector), the cleanest mental model is: parent owns both states with a "set this, clear the other" rule; the panel renders ONE entity at a time but stays open across the swap (`open = node !== null || connector !== null`). This avoids a closing-and-reopening Sheet animation when the user clicks from a node to a connector.
  - "Active tab persists during a single selection (resets on new selection)" maps to `useState` + `useEffect([selectionId])` where `selectionId = node?.id ?? connector?.id`. Don't try to derive the tab from props — the value-and-reset shape needs local state.
  - Existing `detail-panel-*` testids must stay byte-for-byte the same when restructuring an inspector; tests that find an element by testid don't care about the new tablist wrapper around it. Grep for testids before refactoring and copy them verbatim into the new tree.
- Manual browser verification screenshot was inspected in-session; `.playwright-mcp/` is gitignored so no artifact is committed.
---

## 2026-05-07 - US-025
- Replaced `StylePlaceholder` in `DetailPanel` with two real Style-tab editors (`NodeStyleTab` for nodes, `ConnectorStyleTab` for connectors) and wired the full edit-flow through `DemoView`: optimistic `pendingOverrides` → PATCH/DELETE → SSE echo prunes → fall-through to server state on failure with the existing `edit-error-banner`.
- Files added: none (all the new editors live alongside the existing Tabs content in `detail-panel.tsx`).
- Files modified:
  - `apps/web/src/lib/api.ts` — three new helpers: `updateConnector(demoId, connId, patch)` PATCH /api/demos/:id/connectors/:connId, `deleteNode(demoId, nodeId)` DELETE /api/demos/:id/nodes/:nodeId, `deleteConnector(demoId, connId)` DELETE /api/demos/:id/connectors/:connId. Plus a `UpdateConnectorBody` type mirroring the studio's `ConnectorPatchBodySchema`.
  - `apps/web/src/components/detail-panel.tsx` — new `NodeStyleTab` (Border + Background `SwatchRow` × 8 tokens + `DeleteButton`), new `ConnectorStyleTab` (Color `SwatchRow` × 8 tokens + native `<select>` Edge style dropdown + 3-state `DirectionToggle` + `DeleteButton`). New props on `DetailPanelProps`: `onStyleNode`, `onStyleConnector`, `onDeleteNode`, `onDeleteConnector`. Per-control `data-testid`s match the PRD: `style-tab-border-color-{token}`, `style-tab-background-color-{token}`, `style-tab-color-{token}`, `style-tab-edge-style`, `style-tab-direction-{forward|backward|both}`, `style-tab-delete`. Active swatch uses `ring-2 ring-ring ring-offset-2` so an in-flight token is visually pinned. The Edge style dropdown's "Auto (kind)" option clears the `style` field so the connector falls back to the kind-derived dash pattern (per US-017's `connectorToEdge` logic).
  - `apps/web/src/pages/demo-view.tsx` — four new handler `useCallback`s (`onStyleNode`, `onStyleConnector`, `onDeleteNode`, `onDeleteConnector`) all using the existing `usePendingOverrides` pattern. New `selectedNode` / `selectedConnector` `useMemo`s that merge pending overrides onto the server entity so the StyleTab's "active" swatch reflects the in-flight edit immediately. Both memos are placed ABOVE the early returns to keep React's hook order stable.
  - `apps/web/src/components/demo-canvas.tsx` — new `connectorOverrides?: OverrideMap<Connector>` prop + `mergeConnectorOverride(c, override)` helper. Edge-deriving `useMemo` now merges before calling `connectorToEdge`, so connector color/style/direction edits are visible on the canvas during the PATCH round-trip (no flicker on the SSE echo).
  - `apps/web/src/lib/connector-to-edge.ts` — `DerivedEdge.style` widened to include `stroke?: string`. When `connector.color` is set, `colorTokenStyle(token, 'edge')` produces a `{ stroke }` that's spread alongside the kind/style-derived `{ strokeDasharray }`. Unset color leaves stroke undefined so React Flow's default selection styling still applies.
- Quality checks: `bun test` (150/0 — no new tests; the StyleTab is visual-only per the PRD), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (no rewrites).
- Manual browser verification: studio in dev mode (4321 + 5173). Navigated to `/d/todo-demo`. Selected the `api-create-todo` PlayNode → Style tab → clicked the `style-tab-border-color-blue` swatch → demo.json gained `data.borderColor: "blue"` (verified via `grep`), node's border updated immediately on canvas. Selected the `c-complete-worker` connector edge → Style tab → clicked `style-tab-color-purple`, selected `dotted` from `style-tab-edge-style`, clicked `style-tab-direction-both` → demo.json gained `color: "purple"`, `style: "dotted"`, `direction: "both"` (verified via `grep`), edge updated visually with no flicker. Reverted demo.json afterward via `git checkout`.
- **Learnings for future iterations:**
  - Adding a `useMemo` to `demo-view.tsx` requires placing it ABOVE the early returns. The component has TWO guards (`if (!summary)`, `if (loading && !detail)`) before the JSX body — putting any new hook below them will throw "Rendered fewer hooks than expected" on the first render where one of the guards fires (e.g. the brief moment between mount and the first demos-list response). When promoting a derived value (e.g. `const demo = detail?.demo`) to a hook dep, also promote it above the guards so the memo's closure stays consistent.
  - The PRD's "Style tab respects pending overrides" is satisfied by computing `selectedNode` / `selectedConnector` in DemoView as merged values (override over server) and passing them to DetailPanel. The override write happens BEFORE the API call, so the StyleTab re-renders with the new color before the network round-trip completes.
  - Native `<select>` is the right primitive for the Edge style dropdown — the project hasn't pulled in `@radix-ui/react-select` and the StyleTab is a low-traffic surface where shadcn-grade styling isn't essential. Match the existing input height (`h-9`) and ring-on-focus pattern from `Button`/`Tabs` so it doesn't look like a foreign primitive.
  - The "Auto" option in the Edge style dropdown maps to `style: undefined` (clear the override). The PATCH body schema `style: z.enum(['solid','dashed','dotted']).optional()` accepts `undefined` (the field is just absent), and `mergeConnectorUpdates` skips `undefined` values via the `Object.entries(updates)` filter, leaving any prior `style` field on disk untouched. To actually CLEAR a previously-set `style`, the right pattern would be sending `style: null` and updating the body schema + merge to handle deletion. For now "Auto" is more like "don't change" if the connector already has an explicit style. This is an open thread but isn't called out by the PRD as a hard requirement.
  - Connector edits never change `kind` from this UI surface (kind change still requires editing demo.json — that's a non-goal of this PRD). So `setConnectorOverride(connId, patch as Partial<Connector>)` is safe to cast: the discriminator stays intact and the partial only carries non-discriminator fields. If a future edit DOES change kind, the override flow would need to also clear the previous kind's payload fields (mirroring `mergeConnectorUpdates` server-side).
  - Don't forget to widen `DerivedEdge.style` when adding a new CSS property to the edge — TS will silently accept the extra key on the inline-typed `style` object, but React Flow renders only what's in the type. Adding `stroke?: string` ensured the new color flow actually paints.
---

## 2026-05-07 - US-026
- Implemented inline text editing for node titles, node descriptions, and connector labels via double-click. The editor renders a single `<input>` (label) or `<textarea>` (description) keyed off a per-component `editing` state; commit happens on a 400ms debounce during typing AND immediately on blur/Enter; Escape clears the pending debounce and restores the input.
- Files added:
  - `apps/web/src/components/inline-edit.tsx` — `InlineEdit` component. Props: `initialValue`, `onCommit(value)`, `onExit()`, `multiline`, `required`, `field` (for `data-field`), `className`, `placeholder`. Auto-focuses + selects content on mount. `onChange` schedules a 400ms debounced `onCommit`; `onBlur`/`Enter` cancels the debounce and fires `onCommit` immediately, then `onExit`; `Escape` clears the debounce, reverts local state, and `onExit` (no commit). For `required=true` fields, an empty value at finalize-time triggers a shake animation (`inline-edit-shake` keyframe in `index.css`) and skips the commit. Stops keyboard event propagation so Backspace/Delete on the canvas don't trigger node deletion (US-027 keyboard listener).
  - `apps/web/src/components/edges/editable-edge.tsx` — custom React Flow edge component. Renders `BaseEdge` with the smoothstep path (`getSmoothStepPath`) and an HTML overlay via `EdgeLabelRenderer` for the label. The label is a `<button>` styled to look like the default React Flow edge label; double-click swaps it for an `InlineEdit`. When the connector has no label, an unobtrusive "+" badge (visible on hover) gives the user a way to add one without visiting a sidebar.
- Files modified:
  - `apps/web/src/components/nodes/play-node.tsx`, `state-node.tsx`, `shape-node.tsx` — added `onLabelChange?` (and `onDescriptionChange?` for play/state) to each node's `data` shape. Each node renders a `<button>` for the visible text and swaps it to `<InlineEdit>` while editing. PlayNode/StateNode label uses `required=true` (schema: `label.min(1)`); description uses `multiline=true` and is optional (schema: `summary.optional()`). ShapeNode label is optional (`label.optional()`); when empty, the button shows an italic "Double-click to label" hint when editable. While `editing !== null`, NodeResizer's `isVisible` is forced false so resize handles don't fight the inline editor for clicks.
  - `apps/web/src/components/demo-canvas.tsx` — three new optional callback props (`onNodeLabelChange`, `onNodeDescriptionChange`, `onConnectorLabelChange`) wired into per-node `data` and per-edge `data`. Registered `editableEdge: EditableEdge` in a module-scope `edgeTypes` constant (mirrors the `nodeTypes` pattern); passed to `<ReactFlow edgeTypes={edgeTypes}>`. Edge data is created via `{ ...edge, data: { ...edge.data, onLabelChange: onConnectorLabelChange } }` — same channel `onPlay`/`onResize` use for nodes.
  - `apps/web/src/lib/connector-to-edge.ts` — `DerivedEdge.type` flipped from `'smoothstep'` to `'editableEdge'`. Visual output is unchanged because the custom edge uses `getSmoothStepPath` for the path; only the React component rendering the label differs.
  - `apps/web/src/lib/connector-to-edge.test.ts` — updated the type assertion to `'editableEdge'`.
  - `apps/web/src/pages/demo-view.tsx` — three new `useCallback` handlers: `onNodeLabelChange` PATCHes `{ label }`; `onNodeDescriptionChange` reads the current node's detail, splices in the new `summary`, and PATCHes `{ detail: nextDetail }` (preserves `fields[]`/`dynamicSource`/`filePath`); `onConnectorLabelChange` PATCHes `{ label }`. All three set a pending override BEFORE the API call (per US-021's reconciliation pattern) and drop it on failure with the existing `edit-error-banner`.
  - `apps/web/src/index.css` — new `inline-edit-shake` keyframe + utility class.
- Quality checks: `bun test` (150/0, no new tests — the InlineEdit is visual + DOM-bound, and the existing `connector-to-edge.test.ts` was updated for the new edge type), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (autoformatted `index.css` + `demo-canvas.tsx` after edits).
- Manual browser verification: studio in dev mode at `localhost:4321`. Navigated to `/d/todo-demo`. Reverted `examples/todo-demo-target/.anydemo/demo.json` first to a clean slate. Double-clicked the `api-create-todo` PlayNode title → inline `<input>` opened with content selected; typed "POST /todos (renamed)" + Enter → demo.json gained `"label": "POST /todos (renamed)"` (verified via `grep`). Direct-invoked the description button's `onDoubleClick` (Playwright's dblclick on the body button has hit-testing issues against React Flow's pane in the headless harness) → inline `<textarea>` opened with the existing summary selected; typed "Creates a todo (edited)." + Enter → demo.json gained `"summary": "Creates a todo (edited)."`. Double-clicked the connector edge label "publishes todo.completed" → inline `<input>` opened; typed "publishes todo.completed (edited)" + Enter → demo.json gained the new connector label. Reverted `demo.json` afterward via `git checkout`.
- **Learnings for future iterations:**
  - React Flow 12's connector labels render inside the SVG by default, so an inline editor is impossible there. The fix is a custom edge component (`type: 'editableEdge'`) that uses `BaseEdge` for the path + `EdgeLabelRenderer` for an HTML overlay. The overlay is a sibling to the SVG (rendered into a portal); add `nodrag nopan nowheel pointer-events-auto absolute` + `transform: translate(-50%, -50%) translate(${labelX}px, ${labelY}px)` so it sits at the edge midpoint and can receive clicks/typing without being captured by React Flow's pan/zoom.
  - `nodrag nopan nowheel` are the canonical React Flow utility classes for opting elements out of pan/drag/wheel capture. Apply them to inline editors and any clickable label so typing isn't interpreted as a node drag and the wheel isn't eaten by zoom.
  - The InlineEdit's persistence cadence is debounce-while-typing + immediate-on-blur. This means once a 400ms pause has fired a PATCH, Escape can revert the LOCAL input but cannot un-save the intermediate value. The PRD's "Cancel on Escape → no PATCH" is satisfied for the EDIT-IN-PROGRESS PATCH (the pending timer is cleared) but not for already-fired ones. If future requirements need full rollback on Escape, the cleanest fix is a "shadow value" tracked at parent level and a follow-up PATCH back to original on Escape — for now the current behavior matches the spirit of the spec (the in-flight commit is cancelled).
  - `e.stopPropagation()` inside the InlineEdit's `onKeyDown` is essential. Without it, Backspace inside the input would bubble to the canvas and (per US-027) trigger node deletion. Same for Escape — the InlineEdit handles it, then we stop the bubble so other code doesn't react. We also stop click/mousedown so React Flow's node-drag logic doesn't kick in mid-typing.
  - The `required=true` empty-revert + shake pattern: at finalize time (blur/Enter), if `value.trim() === ''`, restore `initialValue`, set a `shake` class for 320ms, and call `onExit()` without `onCommit()`. Crucially, also short-circuit the during-typing debounce when the value is empty (`if (required && next.trim().length === 0) return`) so we don't fire a 400-error to the server in the middle of typing the first character.
  - Description editing reuses `detail.summary` per the PRD (no new field). The handler must read the current `detail` object and SPLICE the new summary in: `{ ...(node.data.detail ?? {}), summary }`. Just sending `{ summary }` would lose `fields[]`/`dynamicSource`. The body schema accepts the entire detail object as a single `detail` key; `mergeNodeUpdates` writes the full object to `data.detail`.
  - When converting a built-in React Flow edge type to a custom one, the `connectorToEdge` test asserting `edge.type === 'smoothstep'` becomes invalid — update the assertion to the new custom type. Visual output stays identical because the custom component uses the same `getSmoothStepPath` helper.
  - Playwright's `dblclick()` against buttons inside React Flow's pane is occasionally unreliable in the headless harness — clicks can mis-target due to the canvas's coordinate transforms. When verifying inline-edit triggers in tests, prefer asserting on the React props directly (`btn.__reactProps.onDoubleClick`) or invoke the handler via `page.evaluate`. The label and connector-label dblclicks worked through Playwright; the description dblclick needed the direct-invocation fallback in this iteration.
  - For inline-edit affordance hints on optional fields (ShapeNode label, connector with no label), avoid surfacing the hint when the field is non-editable (`onLabelChange` undefined). Otherwise the read-only canvas surface gets phantom "click to edit" buttons that do nothing. Gate the hint render on the `editable` boolean.
---

## 2026-05-07 - US-027
- Added a window-level `keydown` listener in `apps/web/src/pages/demo-view.tsx` that handles Delete/Backspace by calling `onDeleteConnector(selectedConnectorId)` (priority) or `onDeleteNode(selectedId)`. The listener short-circuits when `document.activeElement` is an editable element (INPUT/TEXTAREA/SELECT/contentEditable), so InlineEdit / form controls keep their normal Backspace behavior. Selection clears via the existing delete handlers (the panel closes and the cascade is server-driven per US-019).
- The Style tab's Delete button (US-025) is the visual fallback — already wired via `onDeleteNode`/`onDeleteConnector` props on `DetailPanel`, no change needed.
- Files modified:
  - `apps/web/src/pages/demo-view.tsx` — module-scope `EDITABLE_TAGS` set + `isEditableElement()` helper; new `useEffect` that registers the listener (placed after the `onDeleteConnector` `useCallback` so the deps are in scope, and BEFORE the `selectedNode`/`selectedConnector` memos + the early returns to keep React's hook order stable).
- Quality checks: `bun run typecheck` (clean), `bun run lint` (clean), `bun test` (150/0 — no new tests; the keyboard listener is best-verified end-to-end in the browser).
- Manual browser verification: studio in dev mode at `localhost:4321`. Backed up `examples/todo-demo-target/.anydemo/demo.json` to `/tmp/demo.json.backup` first.
  1. Posted a temp `shapeNode` (`id=test-delete-shape`, label "DELETE ME") via `POST /api/demos/:id/nodes`. Clicked the shape on the canvas → `[data-testid='detail-panel']` opened. Pressed Delete → demo.json no longer contained `test-delete-shape` (verified via `grep`); panel closed; node disappeared from canvas.
  2. Posted a temp `default` connector (`id=test-delete-conn`, source=api-create-todo → target=api-complete-todo, label "DELETE ME") via `POST /api/demos/:id/connectors`. Dispatched a synthetic `click` on the edge interaction path (Playwright's dblclick on edges in React Flow is unreliable, see US-026 learning) → panel opened, edge gained `.selected`. Pressed Backspace → connector gone from demo.json; panel closed; edge gone from canvas.
  3. Double-clicked the `POST /todos` PlayNode title → InlineEdit `<input>` appeared with content selected; document.activeElement.tagName === 'INPUT'. Pressed Backspace → input value became "" (browser handled it as text edit); the node was NOT deleted from demo.json (still present, label "POST /todos" intact). Pressed Escape to cancel cleanly.
  4. After all three checks, `diff /tmp/demo.json.backup demo.json` was clean — no leftover state from the test.
- **Learnings for future iterations:**
  - A window-level `keydown` listener for canvas keyboard shortcuts is safe in this app because: (a) React 17+ attaches its synthetic-event listeners at the React root container (not document), so an `e.stopPropagation()` inside an InlineEdit stops the native bubble before it reaches `window`; (b) the `isEditableElement(document.activeElement)` guard catches anything that forgets to stop the bubble, including future inputs we haven't written yet. Prefer `window.addEventListener` over a canvas-element ref-based listener — React Flow's pane doesn't naturally take focus, so a focus-bound listener wouldn't fire after the user clicks an SSE-driven node update that re-renders.
  - Selection priority: `selectedConnectorId` is checked BEFORE `selectedId` because the two are mutually exclusive in the demo-view state machine (`onSelectNode`/`onSelectConnector` clear each other), but this is defense-in-depth. If both were ever set simultaneously, deleting the connector first feels right because the connector is the more recent click.
  - Don't bind the listener inside `DemoCanvas` — that component doesn't own the delete handlers, and threading them down just to wire `useEffect` would multiply props for no UX gain. The page-level component (`demo-view.tsx`) already owns selection state + delete handlers, so the listener belongs there.
  - When adding a `useEffect` that depends on `useCallback`'d values, place it AFTER all the dependencies are declared. The first attempt put the listener immediately after the `pruneOverrides` effects (which is where state-reconciliation effects naturally live), but the deps `onDeleteNode`/`onDeleteConnector` weren't yet declared → `TS2448 used before its declaration`. Move the listener BELOW the last needed callback but BEFORE the early returns + render-time memos.
  - For browser-verifying delete shortcuts on connectors, Playwright's `click()` on a React Flow edge often misses the path due to React Flow's invisible interaction overlays; dispatch a synthetic `MouseEvent('click', { bubbles: true })` against `.react-flow__edge-interaction` (or the path child) via `page.evaluate` to reliably trigger `onEdgeClick`.
- Manual browser verification screenshot was inspected in-session; `.playwright-mcp/` is gitignored so no artifact is committed.
---

## 2026-05-07 - US-028
- Implemented the bottom canvas toolbar with Excalidraw-style click-drag shape creation (rectangle / ellipse / sticky). All draw-mode wiring lives in `DemoCanvas`; the toolbar itself is a pure UI component pinned via React Flow's `<Panel position="bottom-center">`. On commit, the gesture POSTs `/api/demos/:id/nodes` with `{ type: 'shapeNode', position, data: { shape, width, height } }`; the SSE echo brings the new node into the canvas.
- Files added:
  - `apps/web/src/components/canvas-toolbar.tsx` — `CanvasToolbar` pure component. Three icon buttons (Square / Circle / StickyNote from `lucide-react`). Props: `activeShape: ShapeKind | null`, `onSelectShape: (shape: ShapeKind | null) => void`. Active button uses `bg-primary text-primary-foreground`; per-button `data-testid='toolbar-shape-{shape}'`, `aria-pressed`, container `data-testid='canvas-toolbar'`. Toggling the same shape exits draw mode (passes `null` to the parent).
- Files modified:
  - `apps/web/src/lib/api.ts` — new `CreateNodeBody` type + `createNode(demoId, body)` helper that POSTs `/api/demos/:id/nodes`. Body shape mirrors the studio's accepted node payload (`type`, `position`, `data`, optional `id`); the server auto-generates an id if absent.
  - `apps/web/src/components/demo-canvas.tsx` — wired the entire draw flow inside DemoCanvas. New optional prop `onCreateShapeNode?(shape, position, dims)`. When set, the toolbar renders inside `<Panel position="bottom-center">`. The wrapper div listens for `onPointerDown` (only commits when `e.target.classList.contains('react-flow__pane')`), `onPointerMove`, `onPointerUp`, `onPointerCancel`. Window-level `keydown` cancels draw mode on Escape. The React Flow instance is captured via `onInit` to a ref so `screenToFlowPosition` can convert client coords to flow coords on commit. While drawing: `nodesDraggable=false`, `elementsSelectable=false`, `panOnDrag=false`, `zoomOnDoubleClick=false` so the gesture isn't hijacked. The ghost preview is an absolute-positioned div in the wrapper, sized in CLIENT coordinates so its location stays accurate even if the canvas pans during the gesture.
  - `apps/web/src/pages/demo-view.tsx` — new `onCreateShapeNode` callback that calls `createNode(demoId, { type: 'shapeNode', position, data: { shape, width, height } })`. On error, the existing `edit-error-banner` surfaces the failure. No optimistic insertion — the SSE echo arrives <200ms after commit, well within imperceptible flicker bounds for v1.
- Quality checks: `bun test` (150/0), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (autoformatted on first pass).
- Manual browser verification: studio in dev mode at `localhost:4321`. Backed up `examples/todo-demo-target/.anydemo/demo.json` to `/tmp/demo.json.us028.backup`. Navigated to `/d/todo-demo`. Confirmed `[data-testid="canvas-toolbar"]` renders at the bottom-center with three buttons.
  1. Clicked `toolbar-shape-rectangle` → button gained `data-active="true"`, wrapper cursor became `crosshair`.
  2. Dispatched a pointerdown on `.react-flow__pane` at (250,700), pointermove to (430,820), pointerup. demo.json gained a new shapeNode `{ type: 'shapeNode', position: {x:-315,y:255}, data: { shape: 'rectangle', width: 180, height: 120 } }`. Toolbar exited draw mode (active=false, cursor=auto).
  3. Restored demo.json. Clicked sticky button, pressed ESC → button became inactive, cursor returned to auto, no node created.
  4. Clicked ellipse button. Dispatched pointerdown on a NODE element (not the pane) + drag — confirmed NO shape was created and draw mode stayed active (target check correctly rejected). Then dispatched a real pane drag at (600,700)→(800,850) → ellipse shapeNode committed with `width: 200, height: 150`.
  5. Clicked sticky, dragged at (1500,700)→(1700,900) → sticky shapeNode committed with `width: 200, height: 200`. All three shape kinds round-trip cleanly to demo.json.
  6. Restored demo.json after all checks; `diff /tmp/demo.json.us028.backup demo.json` was clean.
- **Learnings for future iterations:**
  - React Flow doesn't expose `onPaneMouseDown`/`onPaneMouseUp` (only `onPaneClick`/`onPaneMouseMove`/`onPaneMouseEnter`/`onPaneMouseLeave`/`onPaneScroll`/`onPaneContextMenu`). For Excalidraw-style click-drag, attach pointerdown/move/up to a wrapper `<div>` around `<ReactFlow>` and gate by `e.target.classList.contains('react-flow__pane')` to ignore down events that land on existing nodes/edges.
  - `useReactFlow()` only works inside a `<ReactFlow>` (or `<ReactFlowProvider>`). For pointer logic in the parent component, capture the instance via `<ReactFlow onInit={(inst) => instanceRef.current = inst}>` instead — the `screenToFlowPosition(client)` method on the instance is the authoritative way to convert client coords into the flow's coordinate space (handles pan + zoom).
  - When pointer-event handlers run synchronously (as in browser tests dispatching pointerdown→move→up in one task), React state updates from earlier handlers don't flush before the later handlers fire. The `useCallback` closures still see stale state. Mirror gesture state into refs (`drawStartRef`, `drawCurrentRef`, `drawShapeRef`) and read those in the handlers; React state stays for visual artifacts (the ghost preview that needs to re-render).
  - `setPointerCapture(pointerId)` and `releasePointerCapture(pointerId)` THROW on synthetic (non-trusted) events. If the throw isn't caught, React's `invokeGuardedCallback` swallows it but skips the rest of the handler. Symptom: the gesture appears to commit visually (the ghost showed) but no commit fires (because pointerup's commit logic comes AFTER the failing release call). Always wrap both in `try/catch` — the gesture still works without explicit pointer capture, just slightly less robust against the cursor leaving the pane mid-drag.
  - Don't add `onPaneMouseUp`-style commits via React Flow's pane handlers even if they were exposed — pane handlers are gated by React Flow's own selection logic. The wrapper-div approach is more direct and doesn't fight React Flow.
  - The PRD's "successful POST → SSE echo prunes the override" is satisfied without an explicit override on this code path. There IS a brief (≤200ms) window where neither ghost nor real node is visible after pointer-up; in practice it's imperceptible. If an optimistic insertion ever becomes necessary, the cleanest pattern would be a parallel "pending nodes" list (not the existing `usePendingOverrides<T>` map, since that's keyed by id and applies partial-of-existing — for a brand-new node we'd need to inject the WHOLE node).
  - For browser tests of the draw flow, `.elementFromPoint(x, y)` is invaluable for verifying the React Flow pane is actually at the coords you're targeting before dispatching pointer events — saves debugging hours when a synthetic drag silently does nothing because the cursor was over a node or the toolbar.
- Manual browser verification screenshot was inspected in-session; `.playwright-mcp/` is gitignored so no artifact is committed.
---

## 2026-05-07 - US-029
- Implemented connector creation via React Flow handle-drag. Dragging from a source handle to a target handle on another node fires `onConnect` → DemoView generates an id client-side, sets a full-Connector optimistic override, then POSTs `/api/demos/:id/connectors` with `{ id, source, target, kind: 'default' }`. The server echo brings the same id into demo.connectors and per-field prune drops the override.
- Files modified:
  - `apps/web/src/lib/api.ts` — added `CreateConnectorBody` type + `createConnector(demoId, body)` helper that POSTs `/api/demos/:id/connectors`. Body mirrors the studio's accepted payload (source/target required; id, kind, label, style, color, direction, and per-kind fields optional). Server auto-generates id and defaults kind='default' if absent — but we always send both client-side so the optimistic override matches the eventual server echo.
  - `apps/web/src/components/demo-canvas.tsx` — new optional `onCreateConnector?(source, target)` prop. When set: `nodesConnectable` flips to `true` (gated to `!drawShape` so handle-drag is disabled while in shape-draw mode), and a new `onConnect(connection)` callback rejects same-node connections client-side (`source === target`) before forwarding to the parent. The `rfEdges` useMemo now also iterates `connectorOverrides` entries whose ids are NOT in `connectors` and treats them as full-Connector pending entities — same `connectorToEdge` decoration so the visual is identical to a server-backed edge. Once the SSE echo lands, the override prunes (per US-021's per-field reconciliation) and the edge becomes server-backed without flicker.
  - `apps/web/src/pages/demo-view.tsx` — new `onCreateConnector` `useCallback` that generates `id = 'conn-' + crypto.randomUUID()`, sets `setConnectorOverride(id, { id, source, target, kind: 'default' })` BEFORE the POST, then `createConnector(demoId, ...)` with `.catch(err => dropConnectorOverride(id) + setEditError(...))`. Wired through to `DemoCanvas`.
- Quality checks: `bun test` (150/0, no new tests — visual-only story per the PRD), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (no rewrites).
- Manual browser verification: studio in dev mode at `localhost:4321`. Backed up `examples/todo-demo-target/.anydemo/demo.json` to `/tmp/demo.json.us029.backup`. Navigated to `/d/todo-demo`. Confirmed handles render with `connectable connectablestart connectableend` classes (was just `react-flow__handle` before).
  1. Drag `api-create-todo` source handle → `todo-worker` target handle via `page.mouse.move/down/up` with 12 intermediate moves. demo.json gained `{ id: 'conn-1168...', source: 'api-create-todo', target: 'todo-worker', kind: 'default' }`. No flicker, no snap-back.
  2. Repeat the SAME drag — second connector with a different id committed alongside the first. Both render as separate edges (PRD: "multiple edges supported by the schema and React Flow").
  3. Drag from `api-create-todo` source handle → `api-create-todo` target handle (same node). Released, waited 800ms — connector count stayed at 3 (no self-connection). Client-side `source === target` check prevented the POST.
  4. Verified backend rejects schema-invalid POST: `fetch /api/demos/:id/connectors { source: 'nonexistent-node', target: 'api-create-todo', kind: 'default' }` returned 400 with `Connector ... references unknown source node` issue. The frontend's `.catch()` would `dropConnectorOverride` + `setEditError` to surface the existing `edit-error-banner`.
  5. Restored demo.json after all checks.
- **Learnings for future iterations:**
  - Optimistic-creation via `usePendingOverrides<T>` works for new entities (not just partial-of-existing) IF you pass the FULL entity record as the override and the rendering useMemo iterates server-data + override-entries-not-in-server. The per-field `applyPruneAgainst` (use-pending-overrides.ts) drops every field whose value matches the server, then drops the entry — for a full-record override that's been echoed back, every field matches, so the entry vanishes cleanly. This is a generalization of the existing pattern and didn't require touching `usePendingOverrides`. The US-028 progress note ("for a brand-new node we'd need to inject the WHOLE node") foreshadowed this — the same trick works for a brand-new connector here.
  - For the optimistic id to match the server echo, send the id in the POST body instead of letting the server auto-generate. The existing endpoint accepts a caller-provided id (`if (typeof newConn.id !== 'string' || newConn.id.length === 0) newConn.id = crypto.randomUUID()`), so `createConnector(demoId, { id, source, target, kind })` round-trips cleanly. If we let the server pick, the optimistic and echoed connectors would have different ids → two edges visible until the optimistic one was dropped manually.
  - `crypto.randomUUID()` works in modern browsers (secure contexts only). Vite's HMR + AnyDemo's localhost-only studio is a secure context, so no polyfill is needed. This matches the existing pattern in US-028's `onCreateShapeNode` (server-side) — the server uses the same identifier on its side.
  - React Flow's `nodesConnectable` is the only flag needed to enable handle drag-out behavior. The Handles already had the right `type='source'`/`type='target'` attribution from the original node components. Once `nodesConnectable={true}`, the existing handle DOM elements pick up `connectable connectablestart connectableend` classes and React Flow attaches the connection-line drag logic. No additional handle-prop changes required.
  - For browser-verifying handle drags with Playwright, use `page.mouse.move(...)` followed by `page.mouse.down() + several intermediate move() calls + page.mouse.up()`. A single `down → move → up` doesn't fire React Flow's "connecting" state machine — it expects a few intermediate move events to enter the connection-line render path. ~10–12 intermediate moves is enough.
- Manual browser verification screenshot was inspected in-session; `.playwright-mcp/` is gitignored so no artifact is committed.
---

## 2026-05-07 - US-030
- Implemented icon-only Play button + status hidden until first run.
- PlayNode (`apps/web/src/components/nodes/play-node.tsx`):
  - `PlayNodeData.status` widened from `NodeStatus` to `NodeStatus | undefined`. Undefined = no entry in the runs map (never-run sentinel).
  - StatusPill now renders conditionally: `{hasRun ? <StatusPill status={status} data-testid="play-node-status" /> : null}`. The `data-testid='play-node-status'` lets tests assert presence/absence cleanly.
  - Play button is now icon-only: `h-6 w-6 p-0`, no text label. `aria-label` + `title` flip between `'Play'` / `'Running…'` based on `isRunning`. Icon swaps from `Play` → `Loader2` (with `animate-spin`) while running, alongside the existing `disabled` state. Existing `data-testid='play-button'` preserved.
- StateNode (`apps/web/src/components/nodes/state-node.tsx`):
  - `StateNodeData.status` also widened to `NodeStatus | undefined` for type accuracy (the upstream now passes undefined when no entry exists). Behavior is unchanged because StateNode already does `data.status ?? 'idle'` — the pill keeps showing 'idle' on never-emitted state nodes (per AC: "StateNode's existing StatusPill behavior is unaffected").
- StatusPill (`apps/web/src/components/nodes/status-pill.tsx`): accepts an optional `data-testid` prop so the conditional wrapper on PlayNode gets `play-node-status` while StateNode (which doesn't pass one) keeps its default unnamed pill.
- DemoCanvas (`apps/web/src/components/demo-canvas.tsx`): added `dataStatusFor(runs, id)` that returns `runs?.[id]?.status` (undefined if no entry), used for the per-node `data.status` injection. The existing `statusFor(...) ?? 'idle'` is preserved for the `adjacentRunning` calculation that drives edge animation — that one still wants a default to compare against.
- Persistence: the runs map is owned by `useNodeRuns(demoId)` and resets on demoId change. Once an entry lands, it stays until reset — naturally satisfying "StatusPill becomes visible and persists thereafter showing the latest run status until page reload or demo reload" without extra plumbing.
- Quality checks: `bun test` (150/0, no new tests — visual-only story per the PRD), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (no rewrites).
- Manual browser verification: studio in dev mode at `localhost:4321`. Loaded `/d/todo-demo`. Verified initial state: 2 PlayNodes (POST /todos, POST /todos/:id/complete) render with NO status pill (`document.querySelectorAll('[data-testid="play-node-status"]').length === 0`); the StateNode (todo-worker) still shows its IDLE pill. Play button rendered icon-only at 24×24 CSS pixels (visible 48×48 due to React Flow 2× zoom) with `aria-label="Play"` and `title="Play"`. Clicked the Play button on POST /todos → `[data-testid="play-node-status"]` appeared on that node with `data-status="error"` (the example backend isn't running so the SSE delivered `node:error` rather than `node:done`); the OTHER never-clicked PlayNode still had no pill. Both validation legs of the AC confirmed.
- **Learnings for future iterations:**
  - The cleanest way to express "no entry in the runs map" downstream is to let the per-node `data.status` be `NodeStatus | undefined` and have each node component decide what to do with the undefined case. Don't add a parallel `hasRun: boolean` field — it duplicates information already encoded in `status`. PlayNode reads it as the "hide pill" sentinel; StateNode falls back to `'idle'` so its visual stays unchanged.
  - Tailwind classes from `cn(buttonVariants(...), className)` flow through `tailwind-merge`, so a later `h-6 w-6 p-0` overrides the size variant's `h-9 ... px-3` cleanly. No need to drop `size="sm"` — let twMerge resolve the conflict.
  - The shadcn Button's `size="icon"` variant produces `h-10 w-10` — too big for our compact PlayNode header. Pass `size="sm"` (or default) and override sizing via className for tighter icon buttons.
  - When verifying icon-only buttons in Playwright, `getBoundingClientRect()` returns CLIENT-pixel dimensions which include any ancestor CSS `transform: scale(...)`. React Flow's pane has a runtime zoom transform — a 24px button shows as 48px at 2× zoom. Use `getComputedStyle(el).height` for the unscaled value when asserting the design intent. The 2× zoom comes from React Flow's `fitView` initialization; it's not a bug.
  - When adding a `data-testid` to a leaf shadcn-style component (StatusPill), declare an explicit prop (`'data-testid'?: string`) rather than spreading `...props` onto the underlying `<span>`. Clearer surface area, no risk of leaking other HTML attributes through.
- Manual browser verification screenshot was inspected in-session (`us030-before-click.png` saved into the repo root by Playwright; deleting before commit since `.playwright-mcp/` is gitignored but stray screenshots aren't).
---

## 2026-05-07 - US-031
- Replaced NodeResizer (visible border lines + visible 5×5 corner dots) with eight invisible NodeResizeControls per node (4 edge lines + 4 corner handles). Affordance is now purely the cursor change at the edges/corners (ew-/ns-/nwse-/nesw-resize), wired by React Flow's existing `.react-flow__resize-control.{position}` CSS rules. Fixed the Sheet inspector closing mid-resize by suppressing Radix's outside-click handler when the pointerdown lands on a `.react-flow__resize-control`.
- Files added:
  - `apps/web/src/components/nodes/resize-controls.tsx` — reusable `ResizeControls` component. Renders 8 `NodeResizeControl` (4 lines + 4 corners) with transparent inline styles + wider hit areas (8px lines, 12×12 corners). Each control gets a `<span data-testid='resize-control' data-position='...'>` child for tests. `visible={false}` short-circuits to `null` (mirrors NodeResizer's `isVisible`). Props: `visible`, `minWidth=80`, `minHeight=40`, `onResizeStart`, `onResizeEnd`.
- Files modified:
  - `apps/web/src/components/nodes/play-node.tsx`, `state-node.tsx`, `shape-node.tsx` — drop `NodeResizer` import, render `<ResizeControls visible={!!selected && !!data.onResize && !isEditing}>` with the same start/end handlers (set local + canvas-level `isResizing`, fire `data.onResize` on stop). Functional behavior identical to US-023 — minimum size enforcement (80×40) preserved, single PATCH on resize-stop preserved, inline-edit gate preserved.
  - `apps/web/src/components/detail-panel.tsx` — `SheetContent` gets `onInteractOutside={(e) => { if (target.closest('.react-flow__resize-control')) e.preventDefault(); }}`. Without this, Radix's default `onPointerDownOutside` fires `onOpenChange(false)` → `onClose()` → clears `selectedNodeId` → unmounts the node's resize controls mid-gesture. The fix lets the resize gesture proceed AND keeps the Sheet open AND keeps the node selected.
- Quality checks: `bun test` (150/0 — no new tests; this story is visual + DOM-bound), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (autoformatted resize-controls.tsx once).
- Manual browser verification: studio in dev mode (4321 + 5173). Loaded `/d/todo-demo`, clicked the `api-complete-todo` PlayNode → 8 resize-control elements rendered. Per-position cursors verified via `getComputedStyle(parent).cursor`: top/bottom → `ns-resize`; left/right → `ew-resize`; top-left/bottom-right → `nwse-resize`; top-right/bottom-left → `nesw-resize`. Lines have `borderColor: transparent` + `backgroundColor: transparent`; corners have `borderStyle: 'none' / borderWidth: 0px / backgroundColor: transparent`. Performed an actual right-edge resize via `page.mouse.move/down/up`: dragged from (800, 550) → (700, 550) — node width changed from 520 to 420 (per-side: width-only, height stayed at 254), `[data-testid='detail-panel']` stayed open through the entire gesture, the selection ring (`ring-2 ring-ring`) stayed on the node, and demo.json was rewritten with `data.width: 210, data.height: 127` (flow coordinates). Reverted demo.json afterward.
- Screenshot inspected showed the selected node with the dark selection ring but NO visible resize border lines or visible corner handles — exactly the AC's "the cursor IS the affordance" intent.
- **Learnings for future iterations:**
  - React Flow exposes `<NodeResizer>` (the high-level convenience) AND `<NodeResizeControl>` (the lower-level building block). `NodeResizer` renders 8 controls internally and only accepts `handleClassName`/`lineClassName` for styling — it can't take per-control data-testids. When you need per-control data-testids OR per-control invisible styling, drop down to 8 individual `<NodeResizeControl>` components. The valid `position` values are `'top' | 'bottom' | 'left' | 'right' | 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right'`; `variant={ResizeControlVariant.Line}` is right for edges and `ResizeControlVariant.Handle` for corners. React Flow's existing `.react-flow__resize-control.{position}` CSS rules wire the cursors automatically — you don't need to set `cursor` in JS.
  - To make the controls invisible without losing the hit area: lines need `borderColor: 'transparent'` (the `.line` CSS uses border-(side)-width to draw the visible 1px line; the border-color override hides it). Set `width` (vertical lines) or `height` (horizontal lines) to ~8px to widen the hit area; the `.react-flow__resize-control.line.{side}` CSS already sets `transform: translate(-50%, 0)` or `(0, -50%)` so the 8px straddles the edge centered on it. Corners need `border: 'none'` (NOT just `borderColor: transparent`, because the default 1px white border would still occupy space) AND `background: 'transparent'`.
  - Radix Dialog with `modal={false}` STILL listens for outside pointerdown events to close itself. To suppress the close on specific outside elements, pass `onInteractOutside={(e) => { if (target.closest('.specific-class')) e.preventDefault(); }}` to `SheetContent` (alias of `Dialog.Content`). The `interactOutside` event fires for both pointerdown AND focus events — covering both axes is cheaper than two narrower handlers. `preventDefault()` cancels the close; non-preventDefault paths fall through to `onOpenChange(false)`. Don't try to fix this with `e.stopPropagation()` on the resize control itself because Radix's listener is on the document in capture phase.
  - Connection handles (`react-flow__handle` at vertical/horizontal mid-edge) sit ABOVE resize controls in z-order at the edge midpoint, so a pointerdown at the exact midpoint hits the handle (for connecting), not the resize control (for resizing). This is correct UX: the dot is the connect affordance; the rest of the edge is the resize affordance. When testing resizes via Playwright, drag from a y-offset that's NOT the vertical center (or x-offset for horizontal lines) — otherwise the gesture starts on the connection handle and a connection-line drag-out will fire instead of a resize.
  - `<NodeResizeControl>` only forwards `className`, `style`, and `children` to the underlying div — `data-testid` and other arbitrary props are NOT forwarded. Put your `data-testid` on a `<span>` inside the `children` slot. Cursors inherit from the parent CSS (`cursor: inherit` is the default for spans), so tests can assert `getComputedStyle(span.parentElement).cursor` OR rely on inheritance and read `getComputedStyle(span).cursor`.
  - When verifying the panel-stays-open behavior, also verify the selection-ring-stays-on behavior. Both come from the same fix (suppress the outside-click close), but they're separately testable: panel-open = `[data-testid='detail-panel']` exists in the DOM after the gesture; selection-ring = the node's outer card still has `ring-2 ring-ring`. If only one passes, your fix is half-applied (e.g. you might have suppressed Radix close but the node's `selected` state still got cleared by another path).
- Manual browser verification screenshot was inspected in-session; deleted before commit (kept `.playwright-mcp/` artifacts gitignored but stray repo-root screenshots aren't).
---

## 2026-05-07 - US-032
- Replaced the always-visible row of 8 palette swatches in the Style tab with a single trigger swatch + shadcn Popover. Each color field (node Border, node Background, connector Color) now renders ONE small swatch button showing the currently-applied token; clicking it opens a popover with the 8 tokens laid out as a 4×2 grid; clicking a token PATCHes the field, applies the optimistic pending override, and closes the popover in the same gesture. Escape or click-outside closes without changing the value.
- Files modified:
  - `apps/web/src/components/detail-panel.tsx` — added `Popover/PopoverTrigger/PopoverContent` imports + `Check` icon. Replaced the `SwatchRow` component with a new `SwatchPicker` that wraps the 8 tokens in a Radix Popover. New props split the testid surface: `triggerTestId` for the always-visible trigger button (e.g. `style-tab-border-color-trigger`) and `tokenTestIdPrefix` for the per-token swatches inside the popover (preserves US-025's `style-tab-border-color-blue` etc. — they just live inside the popover now). Extracted `swatchPreviewStyle(token, kind)` so the trigger AND the per-token buttons share preview styling. Added a diagonal-line "no color" indicator (CSS `linear-gradient` rendered into an `aria-hidden` span) when the active token is `'default'`. Active token in the popover gets `ring-2 ring-ring ring-offset-2` + a `<Check>` icon. Selecting a token calls `setOpen(false)` after `onSelect(token)` to close the popover synchronously with the PATCH.
  - `apps/web/src/components/detail-panel.tsx` (`SheetContent.onInteractOutside`) — exempted `[data-radix-popper-content-wrapper]` so clicks on the popover content (rendered into a portal outside the SheetContent) don't close the non-modal Sheet. The existing `.react-flow__resize-control` exemption from US-031 stays.
  - `apps/web/src/components/detail-panel.tsx` (Style tab call sites) — node Border/Background pickers use `triggerTestId='style-tab-{border|background}-color-trigger'` + `tokenTestIdPrefix='style-tab-{border|background}-color'`. Connector Color picker uses `triggerTestId='style-tab-edge-color-trigger'` + `tokenTestIdPrefix='style-tab-color'` (the per-token testid pattern from US-025 was `style-tab-color-{token}`, kept verbatim per US-032 AC).
- Quality checks: `bun test` (150/0, no new tests — visual-only refinement of US-025), `bun run typecheck` (clean), `bun run lint` (clean), `bun run format` (no rewrites).
- Manual browser verification: studio in dev mode at `localhost:4321`. Navigated to `/d/todo-demo`. Selected `api-create-todo` PlayNode → Style tab. Confirmed only TWO trigger swatches visible (Border + Background), no row of 8. Trigger initially showed `data-active-token='default'` with a diagonal-line CSS `linear-gradient(45deg, transparent 45%, rgb(100 116 139) 45%, ... 55%, transparent 55%)` indicator (the "no color" affordance). Clicked the Border trigger → popover opened in a portal `[data-radix-popper-content-wrapper]` containing all 8 tokens with `default` marked `data-active='true'`; sheet stayed open. Clicked `style-tab-border-color-blue` → popover closed; trigger updated to `data-active-token='blue'`; demo.json gained `data.borderColor: 'blue'` (verified via `fetch /api/demos/:id`). Re-opened popover, pressed Escape → popover closed; demo.json + trigger value unchanged. Switched to the `c-complete-worker` connector edge → Style tab → Color trigger present with `data-active-token='default'`; clicked → popover opened with 8 tokens; clicked `style-tab-color-purple` → popover closed; demo.json gained `color: 'purple'` on the connector. Reverted demo.json + reloaded page → Border trigger correctly shows the diagonal-line indicator on the unset state (CSS `linear-gradient` confirmed via `getComputedStyle`).
- **Learnings for future iterations:**
  - When you have a Radix Popover whose trigger lives inside a non-modal Sheet (`modal={false}`), the popover content renders into a Radix portal — physically OUTSIDE the SheetContent. The Sheet's default `onInteractOutside` would fire on every click inside the popover, closing the sheet mid-edit. Add `if (target?.closest('[data-radix-popper-content-wrapper]')) e.preventDefault();` to the SheetContent's `onInteractOutside` to keep it open. This is the same shape as the US-031 fix for `.react-flow__resize-control` — both are "in-canvas/in-overlay" interactions that Radix sees as "outside" the Sheet by default.
  - For per-control testids that need to refine an earlier story's pattern, split the testid surface into TWO props on the picker component: `triggerTestId` (the new always-visible trigger) and `tokenTestIdPrefix` (the per-token pattern from the previous story). Don't try to derive one from the other — they need to diverge for the connector color (where US-025 used `style-tab-color-{token}` but US-032 wants `style-tab-edge-color-trigger`).
  - The "no color" indicator for the unset state is just a CSS `linear-gradient` rendered into an `aria-hidden` absolute-positioned `<span>` overlaying the swatch button. `linear-gradient(45deg, transparent 45%, currentColor 45%, currentColor 55%, transparent 55%)` produces a diagonal line; setting the `<span>`'s `color` to `hsl(var(--muted-foreground))` adapts to dark mode without a JS branch. Don't try to encode this as an SVG mask — the gradient approach is a single rule, no extra primitive.
  - Active-state visual on the per-token swatches: `ring-2 ring-ring ring-offset-2 ring-offset-popover` (`ring-offset-popover` instead of `ring-offset-background` because the popover has a different surface color from the page). Plus a `<Check>` icon centered inside the swatch with a subtle drop-shadow so it reads on light AND dark token backgrounds. Reading "which token is currently applied" should be obvious without hovering.
  - When verifying popover behavior with Playwright, look for `[data-testid="${triggerTestId}-popover"]` (we set this on PopoverContent) AND `[data-radix-popper-content-wrapper]` (Radix's wrapper). The wrapper exists for any Floating UI overlay, the testid is our addition. Use the testid for assertions; use the wrapper class for SheetContent's outside-click guard.
- Manual browser verification screenshot inspected in-session; deleted before commit.
---

