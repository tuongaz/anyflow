# Ralph Progress Log
Started: Sun 17 May 2026 11:55:53 AEST
---

## 2026-05-17 - US-001
- Changed label text from "Name" to "Flow Name" in the export dialog's name input field
- Files changed: apps/web/src/components/export-dialog.tsx (line 125)
- **Learnings for future iterations:**
  - The export dialog at apps/web/src/components/export-dialog.tsx uses data-testid attributes for all inputs — these must remain unchanged when updating labels
  - Typecheck covers all workspaces via `bun run typecheck` with --filter '*'
---

## 2026-05-17 - US-002
- Converted handleCopy in export-dialog.tsx from `.then()` to async/await with try/catch
- Files changed: apps/web/src/components/export-dialog.tsx (handleCopy function)
- **Learnings for future iterations:**
  - The handleCopy function was using `.then()` with no `.catch()` — clipboard errors were silently swallowed; async/await + try/catch is the pattern to use
  - navigator.clipboard.writeText can throw in non-secure contexts or if permission is denied; always wrap with try/catch and log
---

## 2026-05-17 - US-003
- Updated FlowCard to use white (#fff) background, border #e4e4e7, box-shadow '0 1px 4px rgba(0,0,0,0.08)', dark text (#18181b) for flow name, and light (#f8fafc) MiniCanvas thumbnail area
- Updated DiscoverSkeletonCard in home.tsx to match white card style with Tailwind `animate-pulse` class and light (#f1f5f9 / #e2e8f0) skeleton colors
- Files changed: apps/viewer/src/components/flow-card.tsx, apps/viewer/src/pages/home.tsx
- **Learnings for future iterations:**
  - apps/viewer has no UI lib skeleton component — use Tailwind `animate-pulse` class on elements for shimmer effect
  - FlowCard is shared between home page DiscoverSection and the /flows page — styling changes affect both places
  - Pre-existing lint error in apps/cloud/lambda/api/index.ts (non-null assertion) — do not treat as a blocker for changes in other packages
---

## 2026-05-17 - US-004
- Rewrote flows.tsx to replace page-based pagination with infinite scroll using IntersectionObserver
- Changed LIMIT from 12 to 20, removed Previous/Next buttons, added sentinel div at bottom
- Added maxWidth 1400px container, updated grid to `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` (xl≈1280px closest Tailwind breakpoint to PRD's 1200px)
- Wrapped each FlowCard in a div with `maxWidth: '600px'` to cap column width
- Uses pageRef/totalPagesRef to avoid stale closures in the IntersectionObserver callback
- loadingRef prevents concurrent fetches; loadMoreError state shows 'Failed to load more' with Retry button
- Updated SkeletonCard to match white card style (consistent with US-003)
- Files changed: apps/viewer/src/pages/flows.tsx
- **Learnings for future iterations:**
  - IntersectionObserver callbacks close over state — use refs (pageRef, totalPagesRef) to read current values instead of re-creating the observer on every state change
  - Tailwind's `xl` breakpoint (1280px) is the closest to PRD's 1200px threshold for 3-column grid; use `xl:grid-cols-3` instead of `lg:grid-cols-3`
  - To cap grid item width at 600px: wrap each item in `<div style={{ maxWidth: '600px', width: '100%' }}>` — CSS grid `max-width` on items caps them within their cell
  - The `/flows` page background changed from dark (#09090b) to light (#f8fafc) to match the white card theme from US-003
---

## 2026-05-17 - US-005
- Fixed MiniCanvas thumbnail rendering blank by changing its outer div from `width:'100%', height:'100%'` to `position:'absolute', top:0, left:0, right:0, bottom:0`
- Added `style={{ background: '#f8fafc' }}` to the ReactFlow component so its own background matches the card theme
- Also fixed pre-existing lint error in apps/cloud/lambda/api/index.ts: `obj.Key!` → `obj.Key?.`
- Files changed: apps/viewer/src/components/mini-canvas.tsx, apps/cloud/lambda/api/index.ts
- **Learnings for future iterations:**
  - `height: '100%'` on a child only works if the parent has an explicit height (e.g. `height: 200px`), NOT if the parent's height is derived from `aspect-ratio`. Use `position: absolute; top:0; left:0; right:0; bottom:0` to fill a relatively-positioned parent whose height comes from aspect-ratio.
  - ReactFlow needs a non-zero computed height on its container to render; the absolute-fill pattern is the reliable fix.
  - The pre-existing lint error in apps/cloud/lambda/api/index.ts must be fixed before committing (biome flags `!` non-null assertions); fix with optional chain `?.`.
---

## 2026-05-17 - US-006
- Replaced the header logo `<div>` in apps/viewer/src/pages/home.tsx with `<Link to="/">` from react-router-dom
- Added `hover:opacity-75 transition-opacity` Tailwind classes and `cursor: 'pointer'` style for interactive feedback
- `Link` was already imported in the file (used for the "View all →" link), so no new import was needed
- Files changed: apps/viewer/src/pages/home.tsx (header logo element)
- **Learnings for future iterations:**
  - home.tsx already imports `Link` from react-router-dom — check existing imports before adding new ones
  - The ViewerHeader component (viewer-header.tsx) already uses `<Link>` for its logo; home.tsx had inconsistently used `<div>`
---
